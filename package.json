{
  "name": "gatehouse",
  "version": "0.1.0",
  "description": "Local-first security gate for npm/npx/bun installs - transparent rules, live public feeds, zero runtime dependencies.",
  "license": "MIT",
  "author": "windro",
  "repository": {
    "type": "git"
  },
  "keywords": [
    "supply-chain",
    "security",
    "npm",
    "malware-analysis",
    "sandbox",
    "osv"
  ],
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "bin": {
    "gatehouse": "dist/src/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "smoke": "node dist/scripts/smoke.js",
    "gatehouse": "node dist/src/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
