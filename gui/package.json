{
  "name": "gatehouse-gui",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone desktop app for Gatehouse - native window over the same gate engine.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "gatehouse-gui": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "launcher": "node packaging/build-launcher.mjs",
    "bundle": "npm run build && npm run launcher && node packaging/bundle.mjs"
  },
  "dependencies": {
    "@webviewjs/webview": "0.4.4",
    "gatehouse": "0.1.0"
  }
}
